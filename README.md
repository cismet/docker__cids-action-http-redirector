# docker__cids-action-http-redirector
asks cids action for request options and a redirection url template -> executes request -> puts the result in the redirection url template and redirects to this url
